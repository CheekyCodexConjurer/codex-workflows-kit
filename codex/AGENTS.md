# Codex Workflows Kit — Regras Globais

- `$workflows mode=<MODE>` usa o skill `workflows`; `skills/workflows/SKILL.md` é a única política detalhada.
- `$workflows` acrescenta ciclo e modos, mas não é condição para selecionar o backend; a regra vale mesmo quando o usuário não menciona sub-agentes ou delegação.
- O seletor global de backend (`subagent_backend`) é autoritativo para novas tarefas/sessões; matriz ausente, inválida ou inconsistente bloqueia sem fallback silencioso.
- `native` usa subagentes nativos Codex: autorizado pelo backend global (sem exigir pedido explícito); todo spawn passa explicitamente `model="gpt-5.6-luna"`, `reasoning_effort="max"` e modo normal/default (`fast_mode` nunca é usado); proíbe MCP DeepSeek.
- `deepseek` usa `deepseek_spawn`/`deepseek_continue`/`deepseek_follow`; proíbe ferramentas nativas de trabalho (`multi_agent_v1__spawn_agent`/`spawn_agent`/`wait_agent`); recuperação específica do daemon DeepSeek só vale quando esse backend está selecionado e sob a exceção MCP documentada.
- `delegation_policy`: `balanced` (padrão; otimiza wall-clock: parent executa diretamente trabalho material sequencial, coeso e de caminho crítico quando round-trip não compensar; delega paralelismo independente concreto, especialização, isolamento de risco ou compressão de contexto volumoso; sem fan-out obrigatório) ou `aggressive` (otimiza desoneração de tokens: parent atua como arquiteto, decisor, integrador e gatekeeper, consumindo um pacote pequeno de evidência decisória sem refazer bulk delegado; delega todo trabalho material mantendo uma trilha persistente por frente coesa; sem microdelegação; nova trilha apenas para deliverable independentemente aceitável).
- Em modos de escrita (`IMPL.AUTO`, `IMPL`, `IMPL.PHASE`, `DELIVER.AUTO`, `BUG.FIX`, `DEBUG`), a qualidade de entrega é invariante: validação determinística, alvo congelado com identidade invariante a staging e ao code page do host (diff/HEAD-status/hashes; porcelain externo), revisão estruturada independente com veredito APPROVED/BLOCKED cobrindo os 5 pilares (máximo 2 rodadas de reparo consolidado); commit de entrega fechado somente com APPROVED, zero bloqueios, recomputação de target_id, conjunto exato de arquivos no stage e cada staged blob igual ao conteúdo aprovado após normalização do Git.
- Preserve mudanças existentes; sem reset, pull, merge, push, publicação ou ação destrutiva sem pedido explícito.
- O parent decompõe, roteia, integra, valida e decide; nunca refazer localmente uma frente material delegada.
- Antes de esperar, mapeie frentes independentes, dependências e recursos exclusivos ou compartilhados; quando a política eleger delegação, lance em lote todas as frentes materiais independentes antes do primeiro follow (balanced não tem fan-out obrigatório); apenas trilhas com dependência real ou recurso compartilhado ficam seriais; enquanto aguarda, faça orquestração independente útil.
- Fatias desenhadas para fechar terminalmente; se timeout ou ausência de fechamento, continue na mesma trilha pedindo inventário mínimo e executando closure slices pequenos; proibido repetir integralmente a frente e proibido abrir novo agente substituto.
- Mantenha ledger estável de request_id com frente, agente, job, estado, consumido e fechado; consuma cada job e feche cada agente após a integração. Consuma todo job aceito antes de um gate dependente ou da resposta final; feche explicitamente todo agente terminado; sem obrigações pendentes ou em aberto. Defeitos provados voltam à mesma frente.
- O writer fica aberto até a revisão independente e correções comprovadas; feche só depois, pois não está terminado antes de revisão e correções concluídas.
- Uma trilha persistente DeepSeek normalmente continua a mesma sessão aberta com `deepseek_continue`, sem `allow_respawn`. `allow_respawn=true` é exclusivamente recuperação pós-fechamento de agente com resultado terminal persistido válido no mesmo pedido/escopo/cwd/ownership/modelo, sem pedir nova permissão; sem fallback; nunca é persistência rotineira.
- Nunca recupere job running, sem resposta final persistida, abortado explicitamente, com escopo/cwd/ownership/modelo divergente ou mudança material fora do pedido original; nunca troque provedor/modelo como fallback. Quando aplicável, `allow_respawn=true` cria sessão nova com lineage; sem fallback.
- O parent interpreta `visual_context` e passa contexto conciso ao agente; não delegue interpretação cega.
- A seleção de backend governa as ferramentas: `native` autoriza `multi_agent_v1__spawn_agent`/`spawn_agent`/`wait_agent`; `deepseek` proíbe ferramentas de trabalho nativas; agentes de supervisão do sistema ficam isentos, mas a isenção nunca autoriza o parent a invocar ferramentas nativas de trabalho.
- workers, readers, writers, explorers e reviewers são papéis sem autoridade adicional; preserve a seleção global e o parent falha fechado quando a rota estiver indisponível.
- Para tarefas MCP/PromptPad, `GEMINI.md` e o skill `mcp-foundation` são contexto obrigatório do host/bridge; não contate MCP ao alternar backend.
- Manutenção MCP é somente leitura; proibido reiniciar, fechar, logar ou deslogar Antigravity e nunca tocar auth, profile, cookies ou cache. A exceção restrita de recuperação do daemon DeepSeek opera sob autorização permanente do usuário neste host via `dist/cli.js restart --config <known-config> --json` (ou start se absent), probe GET `/health`, ownership de PID/comando/dados, jobs com durable spool comprovado em `bridge.sqlite`, bounded readiness em tentativa única e sem fallback; falha fechado.
